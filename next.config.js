/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "via.placeholder.com",
      "upload.wikimedia.org",
      "i.pravatar.cc",
      "picsum.photos",
    ],
  },
};

module.exports = nextConfig;
