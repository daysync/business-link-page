# DaySync Business Link Page Configuration
# Copy this file to .env.local and update with your actual values

# REQUIRED: Main DaySync API Base URL
# This is your backend API server URL
DAYSYNC_API_BASE_URL=https://api.daysync.pro

# REQUIRED: Master Username (fallback for development)
# Used when no username is provided in the URL
NEXT_PUBLIC_MASTER_USERNAME=john-doe

# OPTIONAL: Environment Configuration
NODE_ENV=development

# OPTIONAL: Analytics Configuration
# Set to 'false' to disable analytics tracking
ENABLE_ANALYTICS=true

# Note: This project focuses on profile viewing and analytics tracking only.
# No API keys needed - all endpoints are public.
# For private analytics data access, use a separate admin dashboard.